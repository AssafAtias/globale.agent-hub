{
  "name": "globale.agent-hub",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/client",
    "packages/runner"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:client": "npm run dev --workspace=apps/client",
    "dev:runner": "npm run dev --workspace=packages/runner",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": { "node": ">=20" }
}
